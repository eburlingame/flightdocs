
export type EndorsementCalculatorProps = {};

const EndorsementCalculator = ({}: EndorsementCalculatorProps) => {
return (
    <div>

    </div>
)
};

export default EndorsementCalculator;

/*

I hold a <none, student, private, commercial, ATP> pilot certificate for <category + class> aircraft.

I am looking to:
- Take a written test
- Retake a written test
- Solo for category + class
- Solo to an airport within 25nm
- Solo to an airport within 50nm (> 25nm)
- Fly solo to a Class B airport
- Fly solo within Class B airspace
- Perform a solo cross country flight
- Take the practical test for <category + class> aircraft]

*/