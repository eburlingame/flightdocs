import Link from 'next/link'
import CardWrapper from '../../../components/CardWrapper.tsx'
import Reg from '../../../components/Reg.tsx'
import Acronym, { AcronymRow } from '../../../components/Acronym.tsx'
import EncodedDataExplainer, {
    EncodedDataWord,
} from '../../../components/EncodedDataExplainer.tsx'
import StandardAtmosphereSlider from '../../../components/widgets/StandardAtmosphereSlider.tsx'

export default CardWrapper

# Computing Course and Groundspeed

As part of filling out a navlog, pilots will need to estimate magnetic course to
fly and ground speed based on the aircraft's performance and winds aloft.

After you've determined your route of flight, start filling in the rest of the
navlog.

## Step 1: Determine True Course Between Legs

## Step 2:

export const references = [
    {
        name: '',
        link: '',
    },
]
